<UserControl x:Class="View.PatientBookingPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="500">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBlueBrush" Color="#1E3A8A"/>
        <SolidColorBrush x:Key="SecondaryBlueBrush" Color="#3B82F6"/>
        <SolidColorBrush x:Key="AccentBlueBrush" Color="#60A5FA"/>

        <Style x:Key="BlueButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SecondaryBlueBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBlueBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryBlueBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BlueTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBlueBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>

        <Style x:Key="BlueComboBoxStyle" TargetType="ComboBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBlueBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>
    </UserControl.Resources>
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Đặt lịch khám" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryBlueBrush}" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Số điện thoại:" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Width="200" Style="{StaticResource BlueTextBoxStyle}" Text="{Binding PhoneNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Kiểm tra" Style="{StaticResource BlueButtonStyle}" Margin="10,0,0,0" Click="CheckPatient_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10" Visibility="{Binding PatientInfoVisibility}">
            <TextBlock Text="Tên bệnh nhân:" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Width="200" Style="{StaticResource BlueTextBoxStyle}"
                     Text="{Binding PatientName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10" Visibility="{Binding PatientInfoVisibility}">
            <TextBlock Text="Giới tính:" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox Width="200" Style="{StaticResource BlueComboBoxStyle}" 
                      SelectedItem="{Binding Gender, Mode=TwoWay}"
                      ItemsSource="{Binding Genders}"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Thời gian bắt đầu:" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Text="{Binding SelectedStartTime, StringFormat='{}{0:HH:mm}'}" FontSize="14" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Thời gian kết thúc:" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Text="{Binding SelectedEndTime, StringFormat='{}{0:HH:mm}'}" FontSize="14" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Đăng ký" Style="{StaticResource BlueButtonStyle}" Click="RegisterAppointment_Click" Width="100" Height="30" Margin="0,0,10,0"/>
            <Button Content="Hủy" Style="{StaticResource BlueButtonStyle}" Click="Cancel_Click" Width="100" Height="30"/>
        </StackPanel>
    </Grid>
</UserControl>