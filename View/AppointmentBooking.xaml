<UserControl x:Class="View.AppointmentBooking"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <!-- Blue theme styles -->
        <SolidColorBrush x:Key="PrimaryBlueBrush" Color="#1E3A8A"/>
        <SolidColorBrush x:Key="SecondaryBlueBrush" Color="#3B82F6"/>
        <SolidColorBrush x:Key="LightBlueBrush" Color="#EFF6FF"/>
        <SolidColorBrush x:Key="AccentBlueBrush" Color="#60A5FA"/>

        <!-- Button Style -->
        <Style x:Key="BlueButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SecondaryBlueBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBlueBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryBlueBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DatePicker Style -->
        <Style x:Key="BlueDatePickerStyle" TargetType="DatePicker">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBlueBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="BlueDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBlueBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource LightBlueBrush}"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource LightBlueBrush}"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>

        <!-- DataGrid Header Style -->
        <Style x:Key="BlueDataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource PrimaryBlueBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBlueBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <!-- DataGrid Cell Style -->
        <Style x:Key="BlueDataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBlueBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource LightBlueBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{StaticResource PrimaryBlueBrush}" 
                Padding="20,15">
            <TextBlock Text="Đặt Lịch Khám - Chọn Bác Sĩ" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Date Selection Panel -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="{StaticResource SecondaryBlueBrush}" 
                BorderThickness="0,0,0,1" 
                Padding="20,15">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center">
                <TextBlock Text="Chọn ngày khám:" 
                           FontSize="16" 
                           FontWeight="SemiBold" 
                           Foreground="{StaticResource PrimaryBlueBrush}" 
                           VerticalAlignment="Center" 
                           Margin="0,0,15,0"/>
                <DatePicker x:Name="AppointmentDatePicker" 
                           Style="{StaticResource BlueDatePickerStyle}" 
                           SelectedDate="{Binding SelectedDate, Mode=TwoWay}" 
                           Margin="0,0,15,0" 
                           MinWidth="150"/>

                <TextBox 
                         MinWidth="200"
                         Margin="1,0,15,0"
                         Padding="5"
                         Text="{Binding DoctorName, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                         VerticalAlignment="Center">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="BorderBrush" Value="Gray"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <ScrollViewer x:Name="PART_ContentHost"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button x:Name="SearchButton" 
                        Content="Tìm Kiếm" 
                        Style="{StaticResource BlueButtonStyle}" 
                        Click="SearchButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Doctor List -->
        <Border Grid.Row="2" 
                Background="White" 
                Margin="20" 
                CornerRadius="8" 
                BorderBrush="{StaticResource SecondaryBlueBrush}" 
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Results Header -->
                <Border Grid.Row="0" 
                        Background="{StaticResource SecondaryBlueBrush}" 
                        Padding="15,10" 
                        CornerRadius="8,8,0,0">
                    <TextBlock x:Name="ResultsHeader" 
                               Text="Danh sách bác sĩ có lịch" 
                               FontSize="16" 
                               FontWeight="SemiBold" 
                               Foreground="White"
                               HorizontalAlignment="Center"/>
                </Border>

                <!-- DataGrid -->
                <DataGrid x:Name="DoctorDataGrid" 
                          Grid.Row="1" 
                          Style="{StaticResource BlueDataGridStyle}"
                          ColumnHeaderStyle="{StaticResource BlueDataGridHeaderStyle}"
                          CellStyle="{StaticResource BlueDataGridCellStyle}"
                          ItemsSource="{Binding AvailableDoctors}"
                          Margin="0,0,0,0">
                    <DataGrid.Resources>
                        <Style x:Key="CenterTextStyle" TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>

                        <DataGridTextColumn Header="Id" Binding="{Binding DoctorId}" Visibility="Collapsed" />
                        <DataGridTextColumn Header="Tên Bác Sĩ" 
                                          Binding="{Binding DoctorName}" 
                                          Width="200"
                                          ElementStyle="{StaticResource CenterTextStyle}">
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Chuyên Khoa" 
                                          Binding="{Binding Specialization}" 
                                          Width="200"
                                          ElementStyle="{StaticResource CenterTextStyle}"/>
                        <DataGridTextColumn Header="Thời Gian Có Lịch" 
                                          Binding="{Binding AvailableDateTime}" 
                                          Width="*"
                                            ElementStyle="{StaticResource CenterTextStyle}"/>
                        <DataGridTemplateColumn Header="Hành Động" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Xem lịch cụ thể" 
                                            Style="{StaticResource BlueButtonStyle}" 
                                            FontSize="12" 
                                            Padding="10,5" 
                                            Click="SelectDoctor_Click"
                                            Tag="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>