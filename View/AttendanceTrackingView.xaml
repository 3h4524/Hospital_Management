<UserControl x:Class="View.AttendanceTrackingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1400">

    <Border Background="White" CornerRadius="8" Padding="25" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="Gray"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Tiêu đề -->
            <TextBlock Text="Quản lý chấm công" FontSize="20" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,20" Grid.Row="0"/>

            <!-- Các nút chức năng -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10" Grid.Row="1">
                <Button Content="Check-in" Width="100" Margin="5" Click="CheckInButton_Click"/>
                <Button Content="Check-out" Width="100" Margin="5" Click="CheckOutButton_Click"/>
                <Button Content="Làm mới" Width="100" Margin="5" Click="RefreshButton_Click"/>
            </StackPanel>

            <!-- Bảng dữ liệu -->
            <DataGrid x:Name="AttendanceDataGrid"
                      Grid.Row="2"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="0"
                      Background="#F9F9F9"
                      BorderThickness="1"
                      BorderBrush="Gray"
                      SelectionMode="Single"
                      CanUserAddRows="False">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F5F5F5"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="16"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="BorderBrush" Value="Gray"/>
                        <Setter Property="Padding" Value="5"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="BorderBrush" Value="Gray"/>
                        <Setter Property="Padding" Value="5"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ngày" Binding="{Binding WorkDate}" Width="150"/>
                    <DataGridTextColumn Header="Giờ vào" Binding="{Binding CheckInTime}" Width="150"/>
                    <DataGridTextColumn Header="Giờ ra" Binding="{Binding CheckOutTime}" Width="150"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="200"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
