<UserControl x:Class="View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:View">
    <Border Width="320" Height="420" Background="White" CornerRadius="8" Padding="25" >
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="Gray"/>
            </Border.Effect>
            <StackPanel Orientation="Vertical" Margin="20">
                <TextBlock Text="Login" FontSize="25" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                <Grid VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Email" HorizontalAlignment="Left" Grid.Row="0" Margin="0,15"/>
                    <TextBox x:Name="EmailBox" Grid.Row="1" Margin="0,5" Width="255" BorderThickness="0,0,0,1" HorizontalAlignment="Left"/>
                    <TextBlock Text="Password" Grid.Row="2" HorizontalAlignment="Left" Margin="0,15"/>
                    <PasswordBox x:Name="PasswordBox" Grid.Row="3" Width="255" Margin="0,5" BorderThickness="0,0,0,1" />
                </Grid>
                <TextBlock Text="Forgot password?" Cursor="Hand" HorizontalAlignment="Right" TextDecorations="Underline"
                   MouseDown="OnForgotPasswordClick"/>
            <Button Content="Login" Background="Black" Foreground="White"
                Height="36" Margin="0,30,0,10" Width="200" Click="Button_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="18">
                            <TextBlock Text="{TemplateBinding Content}"
                                       Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontWeight="DemiBold"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <TextBlock Text="Not a member? Sign up" Cursor="Hand" Margin="0,10,0,0" HorizontalAlignment="Center"
                   TextDecorations="Underline" MouseDown="OnRegisterClick"/>
            </StackPanel>
        </Border>
</UserControl>