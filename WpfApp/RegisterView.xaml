<UserControl x:Class="View.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:View"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors" 
    mc:Ignorable="d">
    <Border Width="320" Height="420" Background="White" CornerRadius="8" Padding="25">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="Gray"/>
        </Border.Effect>
        <StackPanel Orientation="Vertical" Margin="20">
            <TextBlock Text="Register" FontSize="25" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top"/>
            <Grid VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Email" HorizontalAlignment="Left" Grid.Row="0" Margin="0,15"/>
                <TextBox Grid.Row="1" HorizontalAlignment="Left" Width="255" Margin="0,5"
                         BorderThickness="0,0,0,1"
                         Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Password" HorizontalAlignment="Left" Grid.Row="2" Margin="0,15"/>
                <PasswordBox x:Name="PasswordBox" Grid.Row="3" Width="255" Margin="0,5" BorderThickness="0,0,0,1"
                             PasswordChanged="PwdChanged"/>
                <TextBlock Grid.Row="4" Text="{Binding Message}" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap"/>
            </Grid>
            <Button Content="Register" Command="{Binding RegisterCommand}" Background="Black"
                    Foreground="White" Height="36" Margin="0,30,0,10" Width="200"/>
            <TextBlock Text="Already a member? Log in" Cursor="Hand" Margin="0,10,0,0"
                       HorizontalAlignment="Center" TextDecorations="Underline">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDown">
                        <i:InvokeCommandAction Command="{Binding GoLoginCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </TextBlock>
        </StackPanel>
    </Border>
</UserControl>