<UserControl x:Class="View.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Margin" Value="0,20,0,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" CornerRadius="8" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="{TemplateBinding Foreground}" TextBlock.FontWeight="SemiBold"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="DarkGray"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="255"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Width" Value="255"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="ErrorStyle">
            <Setter Property="Foreground" Value="Red"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </UserControl.Resources>

    <Border Width="320" Height="700" Background="White" CornerRadius="8" Padding="25">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="Gray"/>
        </Border.Effect>
        <StackPanel Orientation="Vertical" Margin="20">
            <TextBlock Text="Register" FontSize="25" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="Full Name" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="FullNameBox">
                <TextBox.Text>
                    <Binding Path="FullName" UpdateSourceTrigger="LostFocus" ValidatesOnDataErrors="True"/>
                </TextBox.Text>
            </TextBox>
            <TextBlock x:Name="FullNameError" Style="{StaticResource ErrorStyle}"
                       Text=""/>

            <TextBlock Text="Role" Style="{StaticResource LabelStyle}"/>
            <ComboBox x:Name="RoleBox" Width="255" Margin="0,0,0,5" SelectedItem="{Binding Role, UpdateSourceTrigger=LostFocus}">
                <ComboBox.Resources>
                    <Style TargetType="ComboBox">
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                </ComboBox.Resources>
            </ComboBox>
            <TextBlock x:Name="RoleError" Style="{StaticResource ErrorStyle}"
                       Text=""/>

            <TextBlock Text="Phone Number" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="PhoneNumberBox">
                <TextBox.Text>
                    <Binding Path="PhoneNumber" UpdateSourceTrigger="LostFocus" ValidatesOnDataErrors="True"/>
                </TextBox.Text>
            </TextBox>
            <TextBlock x:Name="PhoneNumberError" Style="{StaticResource ErrorStyle}"
                       Text=""/>

            <TextBlock Text="Email" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="EmailBox">
                <TextBox.Text>
                    <Binding Path="Email" UpdateSourceTrigger="LostFocus" ValidatesOnDataErrors="True"/>
                </TextBox.Text>
            </TextBox>
            <TextBlock x:Name="EmailError" Style="{StaticResource ErrorStyle}"
                       Text=""/>

            <TextBlock Text="Password" Style="{StaticResource LabelStyle}"/>
            <PasswordBox x:Name="PasswordBox" LostFocus="PasswordBox_PasswordChanged"/>
            <TextBlock x:Name="PasswordError" Style="{StaticResource ErrorStyle}" Text=""/>

            <TextBlock Text="Confirm Password" Style="{StaticResource LabelStyle}"/>
            <PasswordBox x:Name="ConfirmPasswordBox" LostFocus="ConfirmPasswordBox_PasswordChanged"/>
            <TextBlock x:Name="ConfirmPasswordError" Style="{StaticResource ErrorStyle}" Text=""/>

            <Button Content="Register" Style="{StaticResource RoundedButtonStyle}" Click="Button_Click"/>
            <TextBlock Text="Already a member? Log in" Cursor="Hand" Margin="0,10,0,0" HorizontalAlignment="Center" TextDecorations="Underline" MouseDown="OnLoginClick"/>
            <TextBlock x:Name="MessageTextBox" Style="{StaticResource ErrorStyle}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</UserControl>